# Prisma（開発: SQLite）
DATABASE_URL="file:./dev.db"

# POST /api/sync の Bearer 認証（Vercel Cron 等の外部トリガー用）
# 本番では必須。未設定の場合、本番環境では /api/sync が 401 を返す。
# 設定画面の手動同期は Server Action 経由のため不要。
# SYNC_API_SECRET="your-random-secret-here"
